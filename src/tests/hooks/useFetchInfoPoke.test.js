// import { useFetchInfoPoke } from '../../hooks/useFetchInfoPoke';
// import { renderHook } from '@testing-library/react-hooks';

describe('Pruebas con Hook useFerchPoke', () => {
    
    test('Debe retornar un arreglo con datos del Pokemon', async() => {
        
    //     const { result, waitForNextUpdate } = renderHook( () => useFetchInfoPoke( 2 ) );
    //     await waitForNextUpdate();

    //     const { data } = result.current;
    //     expect( data.length ).toEqual(3);
    })
    

})