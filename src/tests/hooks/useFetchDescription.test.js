// import { useFetchDescription } from '../../hooks/useFetchDescription';
// import { renderHook } from '@testing-library/react-hooks';

describe('Pruebas con Hook useFerchPoke', () => {
    
    test('Debe retornar un arreglo con datos del Pokemon', async() => {
    
    //     const id = 4;

    //     const lg = 'es';

    //     const { result, waitForNextUpdate } = renderHook( () => useDescription( id, lg ) );
    //     await waitForNextUpdate();

    //     const { desc } = result.current;
    //     expect( desc.length ).toEqual(1);
    })
    

})