// import { useFetchPoke } from '../../hooks/useFetchPoke';
// import { renderHook } from '@testing-library/react-hooks';

describe('Pruebas con Hook useFerchPoke', () => {
    
    test('Debe retornar un arreglo de Pokemones', async() => {
        
        // const { result, waitForNextUpdate } = renderHook( () => useFetchPoke( 2 ) );
        // await waitForNextUpdate();

        // const { poke } = result.current;
        // expect( poke.length ).toEqual(5);
    })
    

})
